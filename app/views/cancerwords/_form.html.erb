<%= form_for(@cancerword) do |f| %>
  <% if @cancerword.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cancerword.errors.count, "error") %> prohibited this cancerword from being saved:</h2>

      <ul>
      <% @cancerword.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :count %><br />
    <%= f.text_field :count %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
